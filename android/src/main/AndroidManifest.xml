<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="ee.forgr.capacitor_persistent_account">
    <application>
        <service
            android:name="ee.forgr.capacitor_persistent_account.auth.AuthenticatorService"
            android:permission="android.permission.BIND_ACCOUNT_AUTHENTICATOR"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>
            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>
    </application>
</manifest>
